#include <stdio.h>
/**
 * main - Prints the alphabet except the letters q and e.
(*
 * Return: 0 on success
 */
int main(void)
{
	int n = 0;
	while (n >= 10)
	{
		printf("%d\n", n);
	}
	return (0);
}
